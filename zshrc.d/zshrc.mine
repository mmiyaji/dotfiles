w
cal
date

export LESS='-R'

export WORKON_HOME=$HOME/.virtualenvs
[ -x "`which virtualenvwrapper.sh 2>/dev/null`" ] && source `which virtualenvwrapper.sh`

export LC_ALL=ja_JP.UTF-8
export LC_CTYPE=ja_JP.UTF-8
